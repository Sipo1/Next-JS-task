import Heading from "./heading"
import styles from "../styles/Footer.module.css"

const Footer = () => {
    return (
      <footer className={styles.footer}>
        <Heading teg = ""h3 text = "Next JS"/>
      </footer>
    )
}

export default Footer